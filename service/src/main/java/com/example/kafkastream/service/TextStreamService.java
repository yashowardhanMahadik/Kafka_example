// src/main/java/com/example/kafkastream/service/TextStreamService.java

package com.example.kafkastream.service;

import org.springframework.scheduling.annotation.Scheduled;
import org.springframework.stereotype.Service;

@Service
public class TextStreamService {

//    private final KafkaProducerService kafkaProducerService;
//    private int counter = 0;
//
//    public TextStreamService(KafkaProducerService kafkaProducerService) {
//        this.kafkaProducerService = kafkaProducerService;
//    }
//
//    @Scheduled(fixedRate = 5000)
//    public void sendTextStream() {
//        String message = "Message " + counter++;
//        kafkaProducerService.sendMessage(message);
//        System.out.println("Sent: " + message);
//    }
}
